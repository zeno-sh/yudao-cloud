<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.dm.dal.mysql.ad.OzonAdCampaignsMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="selectPage2" resultType="cn.iocoder.yudao.module.dm.controller.admin.ad.vo.OzonAdCampaignsPageRespVO">
        SELECT
        <!-- 选择分组维度 -->
        <choose>
            <when test="reqVO.groupType == 'day'">
                DATE(date) AS date,
            </when>
            <when test="reqVO.groupType == 'week'">
                YEARWEEK(date, 1) AS date,
            </when>
            <when test="reqVO.groupType == 'month'">
                DATE_FORMAT(date, '%Y-%m') AS date,
            </when>
            <otherwise>
                DATE(date) AS date,  <!-- 默认按天分组 -->
            </otherwise>
        </choose>
        client_id,
        SUM(views) AS views,
        SUM(clicks) AS clicks,
        SUM(money_spent) AS money_spent,
        AVG(avg_bid) AS avg_bid,
        SUM(orders) AS orders,
        SUM(orders_money) AS orders_money
        FROM dm_ozon_ad_campaigns
        WHERE 1=1
        <!-- 使用 clientIds 数组进行 IN 查询 -->
        <if test="reqVO.clientIds != null">
            AND client_id IN
            <foreach collection="reqVO.clientIds" item="clientId" open="(" separator="," close=")">
                #{clientId}
            </foreach>
        </if>
        <!-- 使用 date 范围进行查询 -->
        <if test="reqVO.date != null">
            AND date BETWEEN #{reqVO.date[0]} AND #{reqVO.date[1]}
        </if>
        <!-- 动态分组 -->
        <choose>
            <when test="reqVO.groupType == 'day'">
                GROUP BY DATE(date), client_id
            </when>
            <when test="reqVO.groupType == 'week'">
                GROUP BY YEARWEEK(date, 1), client_id
            </when>
            <when test="reqVO.groupType == 'month'">
                GROUP BY DATE_FORMAT(date, '%Y-%m'), client_id
            </when>
            <otherwise>
                GROUP BY DATE(date), client_id  <!-- 默认按天分组 -->
            </otherwise>
        </choose>
        ORDER BY money_spent DESC
    </select>

</mapper>