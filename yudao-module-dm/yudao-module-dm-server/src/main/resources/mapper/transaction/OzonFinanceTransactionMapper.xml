<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.dm.dal.mysql.transaction.OzonFinanceTransactionMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="selectServicePage" resultType="cn.iocoder.yudao.module.dm.controller.admin.transaction.vo.OzonServiceTransactionRespVO">
        SELECT
        <!-- 选择分组维度 -->
        <choose>
            <when test="reqVO.groupType == 'day'">
                DATE(operation_date) AS operation_date,
            </when>
            <when test="reqVO.groupType == 'week'">
                YEARWEEK(operation_date, 1) AS operation_date,
            </when>
            <when test="reqVO.groupType == 'month'">
                DATE_FORMAT(operation_date, '%Y-%m') AS operation_date,
            </when>
            <otherwise>
                DATE(operation_date) AS operation_date,  <!-- 默认按天分组 -->
            </otherwise>
        </choose>
        client_id,
        SUM(amount) AS amount
        FROM dm_ozon_finance_transaction
        WHERE 1=1
            AND type = 'services' AND operation_type  &lt;&gt;  'OperationElectronicServiceStencil'
        <!-- 使用 clientIds 数组进行 IN 查询 -->
        <if test="reqVO.clientIds != null">
            AND client_id IN
            <foreach collection="reqVO.clientIds" item="clientId" open="(" separator="," close=")">
                #{clientId}
            </foreach>
        </if>
        <!-- 使用 operationDate 范围进行查询 -->
        <if test="reqVO.operationDate != null">
            AND operation_date BETWEEN #{reqVO.operationDate[0]} AND #{reqVO.operationDate[1]}
        </if>
        <!-- 动态分组 -->
        <choose>
            <when test="reqVO.groupType == 'day'">
                GROUP BY DATE(operation_date), client_id
            </when>
            <when test="reqVO.groupType == 'week'">
                GROUP BY YEARWEEK(operation_date, 1), client_id
            </when>
            <when test="reqVO.groupType == 'month'">
                GROUP BY DATE_FORMAT(operation_date, '%Y-%m'), client_id
            </when>
            <otherwise>
                GROUP BY DATE(operation_date), client_id  <!-- 默认按天分组 -->
            </otherwise>
        </choose>
        ORDER BY operation_date DESC
    </select>
</mapper>